# Changelog

All notable changes to `queuestorm-investigator` are documented here.
The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and the project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.1.0] — 2026-06-26

### Added
- Project skeleton (`app/`, `tests/`, toolchain config).
- Empty but importable `app/` package with `create_app()` factory.
- Tooling: `ruff` (lint + format), `mypy --strict`, `pytest`.
- `pyproject.toml`, `requirements*.txt`, `.gitignore`, `.editorconfig`,
  `.dockerignore` (empty), `.env.example` (empty), `README.md`, `LICENSE`, `CHANGELOG.md`.
